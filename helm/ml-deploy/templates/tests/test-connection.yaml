apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ml-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "ml-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ml-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
